NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Optional: nicer bot lines in Quick Play (gpt-4o-mini). Without it, template lines still play.
OPENAI_API_KEY=

# Optional: verbose bot-turn logs in browser + server (trySubmitLlmBotLine, relay_room_tick).
# NEXT_PUBLIC_RELAY_BOT_DEBUG=1
